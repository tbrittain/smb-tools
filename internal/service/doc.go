// Package service implements business logic that crosses store boundaries.
// Service methods orchestrate across multiple stores or the save game reader.
// They receive all dependencies via constructor — no global state.
package service
